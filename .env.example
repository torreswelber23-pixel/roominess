# Copyright (c) Meta Platforms, Inc. and affiliates.
#
# This source code is licensed under the MIT license found in the
# LICENSE file in the root directory of this source tree.

# Auth0 bypass (development only)
# Set to 'true' to skip Auth0 login during local development. When enabled,
# the app uses TP_CONTACT_EMAIL as the user identity instead of Auth0.
# This is ignored in production (NODE_ENV !== 'development').
# Note: data created with bypass is stored under TP_CONTACT_EMAIL. If you later
# switch to Auth0 with a different email, you won't see that data — redo the
# embedded signup to create data under your Auth0 email.
# BYPASS_AUTH='true'

# Safe local preview (development only)
# Prevents placeholder credentials from being sent to Meta, Postgres or Ably.
# DEMO_MODE='true'

# Auth0 Configuration
# Create an application at https://auth0.com/ and copy these values from its settings.
# The Auth0 SDK for Next.js reads these automatically.
APP_BASE_URL='http://localhost:3000'
AUTH0_DOMAIN='your-tenant.auth0.com'
AUTH0_CLIENT_ID='your-auth0-client-id'
AUTH0_CLIENT_SECRET='your-auth0-client-secret'
AUTH0_SECRET='generate-with-openssl-rand-hex-32'

# Meta / Facebook Configuration
# Get these values from the Meta Developer Dashboard: https://developers.facebook.com/apps/
FB_APP_ID='your-facebook-app-id'
FB_APP_SECRET='your-facebook-app-secret'
FB_GRAPH_API_VERSION='v22.0'
FB_REG_PIN='your-registration-pin'
FB_VERIFY_TOKEN='your-webhook-verify-token'

# Ably Configuration
# Get this from your Ably dashboard: https://ably.com/accounts
ABLY_KEY='your-ably-api-key'

# Contact Information
TP_CONTACT_EMAIL='your-contact-email@example.com'

# Privacy Policy (optional)
# If set to an http(s) URL, the /privacy route redirects there. Leave unset (or
# set to any non-URL value) to serve the built-in placeholder page at /privacy.
# PRIVACY_POLICY_URL='https://example.com/privacy'
# If PRIVACY_POLICY_URL is not set (or not a valid URL), the app will render
# the HTML below as the privacy policy page. Use this for quick testing without
# hosting a separate policy page.
# PRIVACY_POLICY_HTML='<h1>Privacy Policy</h1><p>Last updated: ...</p>'

# Database (auto-configured when using Vercel + Neon integration)
# POSTGRES_URL='postgres://...'

# AI qualification (optional; rules_v1 works without an external provider)
# AI_PROVIDER='openai-compatible'
# AI_BASE_URL='https://api.example.com/v1'
# AI_API_KEY='your-ai-provider-api-key'
# AI_MODEL='your-model-name'

# Voice agent (optional; required only when implementing STT/LLM/TTS automation)
# VOICE_PROVIDER='your-voice-provider'
# VOICE_PROVIDER_API_KEY='your-voice-provider-api-key'
# VOICE_MODEL='your-voice-model'

# CRM and calendar connectors (optional; future modules)
# CRM_PROVIDER='your-crm-provider'
# CRM_API_KEY='your-crm-api-key'
# CALENDAR_PROVIDER='google-or-outlook'

# Note: Copy this file to .env.local and replace the placeholder values with your actual secrets.
# Never commit .env.local to version control.

